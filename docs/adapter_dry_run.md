# Adapter Dry Run

Dry-run may reveal the shape of a repair or mutation.

Dry-run may not perform it.

ASF-R v0.3 adapter dry-run models filesystem, GitHub, and agent actions without
writing files, mutating repositories, updating memory, releasing software, or
calling live mutation APIs.
