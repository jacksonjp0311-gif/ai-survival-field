# Public Demo

Run:

```powershell
python -m asf.cli demo
```

The demo compresses ASF-R into a runnable explanation:

```text
ASF-R turns AI failure into evidence-gated recovery.
```

The demo is read-only and non-mutating.
