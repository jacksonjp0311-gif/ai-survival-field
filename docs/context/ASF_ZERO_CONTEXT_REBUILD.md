# ASF Zero-Context Rebuild

A fresh session must recover the current ASF-R state from:

```text
docs/context/latest-asf.json
docs/releases/
README.md
tests/
```

Zero-context law:

```text
If a fresh session cannot rehydrate the runtime state, the system is not mature.
```

The latest pointer rehydrates state only. It does not grant authority.
