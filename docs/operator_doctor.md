# Operator Doctor

The doctor command checks scaffold health and non-claim lock presence.

```powershell
python -m asf.cli doctor
```

Doctor checks are diagnostic only. They grant no action authority.
