# Enforcement Report

Enforcement reports bind adapter events to runtime decisions.

The report includes event hash, decision hash, policy hash, route hash,
rehydration hash, adapter mode, simulated effect, wound ID when blocked, and
ledger record hash.

In v0.3:

```text
mutation_performed: false
```

Dry-run success is not production permission.
